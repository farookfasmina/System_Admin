{
  "name": "ledger-todo-app",
  "version": "1.0.0",
  "description": "Ledger — a minimal task journal built with vanilla HTML/CSS/JS as part of an Advanced Git & DevOps assignment.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve src",
    "lint": "eslint src/scripts --no-error-on-unmatched-pattern",
    "build": "node -e \"console.log('Static site — nothing to bundle. Build check passed.')\"",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^9.9.0"
  }
}
